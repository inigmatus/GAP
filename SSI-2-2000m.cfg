//SSI flight to 2000m 

CONTRACT_TYPE
{
//Contract Descriptions
	name = SSI-2-2000m
	title = Test flight to 2000m
	group = SSI Aerospace
	agent = SSI Aerospace
	description = Take off and landing is easy. Flying up to 2000m and landing back at the runway... is an entirely different matter.
	synopsis = Simply launch an airplane from the runway, and get up into the air to 2000m and land back at the KSC runway without destroying the aircraft.
	notes = Enjoy the view!
	completedMessage = Flight Completed!

//Contract Limits
	minExpiry = 7.0
	maxExpiry = 7.0
	deadline = 0
	cancellable = true
	declinable = true
	autoAccept = false
	prestige = Trivial
   	targetBody = Kerbin
   	maxCompletions = 1
   	maxSimultaneous = 1

//Contract Rewards
 	rewardScience = 3.0
  	rewardReputation = 5.0
  	rewardFunds = 8000.0
 	failureReputation = 5.0
  	failureFunds = 10000.0
  	advanceFunds = 2000
	weight = 10.0

//Requirements for Contract to Appear	
	REQUIREMENT
	{
		name = CompleteContract
		type = CompleteContract
		contractType = SSI-1-FirstFlight
		minCount = 1
	}

//Requirements for Contract Completion
	PARAMETER
	{
		name = HasCrew
		type = HasCrew
		hideChildren = true
		trait = Pilot
		minCrew = 1
		title = You must have a volunteer. This time he must be a certified pilot.
	}

	PARAMETER
	{
		name = ReachState
		type = ReachState
		title = Fly up to 2000m
		hideChildren = true
		situation = FLYING
		minAltitude = 2000
		targetBody = Kerbin
		disableOnStateChange = true
		completeInSequence = true
	}

	//Recovery parameters	
	PARAMETER
	{
		name = ReachState
		type = ReachState
		title = Land safely
		hideChildren = true
		situation = LANDED
		maxSpeed = 0.0
		targetBody = Kerbin
		disableOnStateChange = false
		completeInSequence = true
	}
	
	PARAMETER
	{
		name = All
		type = All
		title = Recover

		PARAMETER
		{
			name = RecoverVessel
			type = RecoverVessel
			title = your airplane
		}
		
		PARAMETER
		{
			name = VesselNotDestroyed
			type = VesselNotDestroyed
			title = without destroying it
		}
		
		PARAMETER
		{
			name = KerbalDeaths
			type = KerbalDeaths
			title = and do not kill anyone in the process
			countMax = 1
		}
		
	}	
	
}
